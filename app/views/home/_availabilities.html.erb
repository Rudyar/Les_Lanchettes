<% @availabilities.order("date").each do |availability|%>
  <tbody>
    <tr>
      <td class="align-middle"><%= availability.date.strftime('%d/%m/%Y') %></td>
      <td class="align-middle"><%= availability.price %> €</span></td>
      <% if availability.available == true %>
        <td class="align-middle"><a class="btn btn-success" id="btn-availabilities">Oui</a></td>
      <% else %>
        <td class="align-middle"><a class="btn btn-danger" id="btn-availabilities">Non</a></td>
      <% end %>
    </tr>
  </tbody>

<% end %>