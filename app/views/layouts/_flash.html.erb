<% flash.each do |type, msg| %>
  <div class="alert alert-<%= type %> alert-dismissable fade show text-center mb-0" role="alert">
    <%= msg %>
  <button type="button" class="close" data-dismiss="alert" aria-label="Close">
    <span aria-hidden="true">×</span>
  </button>
  </div>
<% end %>